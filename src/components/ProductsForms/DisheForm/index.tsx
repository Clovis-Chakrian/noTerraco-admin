import { View, Text } from 'react-native';
import styles from './styles';

function DisheForm () {
  return (
    <View>
      <Text> Tela IDisheCard </Text>
    </View>
  );
};

export default DisheForm;